StylesPath = .config/vale_styles
MinAlertLevel = suggestion

# External packages
Packages = alex, Google, proselint, Readability,
# Only Markdown and .txt files; change to whatever you're using.
[*.{md,txt}]
# List of styles to load.
BasedOnStyles = alex, Google, proselint, Readability, 

